{
  "name": "aitoolbox",
  "private": true,
  "version": "3.1.0",
  "description": "AI chat enhancement Chrome extension — dev metadata only, not shipped in the zip.",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'"
  }
}
